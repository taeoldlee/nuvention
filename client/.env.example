# API URL (default: /api via Vite proxy)
VITE_API_URL=/api

# Google Places API key (enables autocomplete search in brand onboarding)
# Without this key, brand onboarding falls back to URL paste import
VITE_GOOGLE_PLACES_API_KEY=your_google_places_api_key
